# EngMon — engmon.monster

`engmon.monster` — **1년 52주 플랜**으로 읽는 주간 영어 매거진. 빌드 도구가 필요 없는 정적 사이트(HTML + CSS + JS)입니다.
MonsterLab(`monsterlab.monster`)의 두 번째 서비스입니다.

## 구조

```
index.html     # 매거진 본문 (표지·주 선택기·목차·섹션·복습·단어장·52주 플랜·알림 신청 폼)
issues.js      # 매거진 데이터(52주 플랜 + 발행된 주의 섹션) — 이 파일이 콘텐츠의 "DB"
magazine.js    # 매거진 렌더링·오디오·받아쓰기·퀴즈·복습 카드·단어장·주 선택기·로드맵
script.js      # 페이지 공통 (한/영 전환, 테마, 강조색, 모바일 메뉴, 문의 폼 전송)
styles.css     # 스타일 (다크/라이트 테마, 강조색 프리셋, 인쇄용, 반응형)
analytics.js   # 방문 분석 (Microsoft Clarity) — 프로젝트 ID를 넣기 전에는 아무 것도 안 함
og.png         # 공유 카드 이미지 (1200×630) — og:image / twitter:image
robots.txt     # 검색엔진 크롤링 허용 + 사이트맵 위치
sitemap.xml    # 사이트맵 (한 페이지, 주 단위 갱신)
smoke-test.js  # 검증 스크립트 (배포 전 `node smoke-test.js`)
browser-test.js# 헤드리스 Chrome 검증 (스크롤·모바일 레이아웃·실제 클릭·네트워크)
CNAME          # GitHub Pages 커스텀 도메인 (engmon.monster)
```

- 프레임워크·패키지·빌드 과정 없음. 파일을 그대로 올리면 동작합니다.
- 외부 의존성 없음(시스템 폰트, CDN 요청 없음). 오디오는 브라우저 내장 음성합성(`speechSynthesis`)을 씁니다.
  단, **방문 분석을 켜면**(프로젝트 ID 입력) Clarity 스크립트 1개를 불러옵니다 — 아래 "방문 분석" 참고.
- `script.js`가 공용 API(`window.MonsterLab`의 `t`·`toast`)와 `langchange` 이벤트를 노출하고,
  `magazine.js`는 그 두 가지로만 연결됩니다.
- `script.js`는 **공용 API를 먼저 노출한 뒤, 기능별로 `guard()` 안에서 초기화**합니다.
  한 기능이 예외로 죽어도 나머지(특히 언어 전환)는 계속 동작하고, 실패한 기능만 콘솔에 남습니다.

> MonsterLab 본체와 분리된 저장소입니다. 홈페이지·서비스 소개는 [monsterlab.monster](https://monsterlab.monster)에 있습니다.

## 1년 52주 플랜

**모든 주가 `issues.js`의 `MAGAZINE_WEEKS`에 들어 있습니다.** 그중 `sections`가 있는 주만 "발행된 주"이고,
나머지는 계획(발행 전)입니다. 지금은 **1~4주만 발행**되어 있고 **5주부터는 아직 발행 미정**이며,
구성이 끝나는 대로 차례로 공개합니다.

| 주 | 분기 | 레벨 | 주제 | 상태 |
| --- | --- | --- | --- | --- |
| W01 | 1 | B1 | 여행 영어 | 발행됨 |
| W02 | 1 | B1 | 직장 영어 | 발행됨 |
| W03 | 1 | B1 | 일상 회화 | 발행됨 |
| W04 | 1 | B2 | 시사·뉴스 영어 | 발행됨 |
| W05 ~ W52 | 1~4 | A2~C1 | 분기별 주제 로드맵 | 발행 예정 |

화면에서 이 플랜이 어떻게 보이는지:

- **표지의 주 선택기** — 52주가 작은 번호 칩으로 나옵니다. 발행된 주는 채워지고, 아직 준비 중인 주는
  점선 테두리입니다. 어느 주든 누르면 그 주로 이동합니다.
- **`#plan` 로드맵(1년 52주 플랜)** — 4분기로 묶어 주마다 주제·제목·레벨·(발행된 주는) 분량을 보여 줍니다.
  지금 보고 있는 주는 강조 표시됩니다. 상단 메뉴의 **플랜**이 이곳으로 갑니다.
- **준비 중인 주** — 본문 대신 "이 주는 아직 준비 중입니다" 안내와 플랜으로 가는 버튼을 보여 줍니다.
  표지 통계와 진행률도 분량 대신 **발행 예정**으로 표시합니다.

### 주(week)를 발행하려면

`issues.js`에서 그 주의 객체에 `published`(발행한 달, `'2026-11'`)와 `sections`(아래 필드 조합)를 채우면 됩니다.
표지·목차·진행률·플랜 화면이 자동으로 "발행됨"으로 바뀌고, 주 선택기 칩도 채워집니다.

```js
{
  week: 5,
  slug: 'week-05',
  quarter: 1,
  published: '2026-11',            /* 발행한 달 — 넣는 순간 "발행됨" */
  level: 'A2',
  theme: { ko: '쇼핑·결제', en: 'Shopping and paying' },
  title: { ko: '옷 가게에서 사이즈와 환불 말하기', en: 'Sizes and refunds without guessing' },
  summary: { ko: '…', en: '…' },
  sections: [ /* 아래 필드 조합 */ ]
}
```

- 주에 적는 값은 `week`·`slug`·`quarter`·`published`·`level`·`theme`·`title`·`summary` 뿐입니다.
  **학습 분량(분)은 적지 마세요** — 표지의 "분"은 섹션 내용에서 자동으로 계산합니다
  (읽기 120단어/분 + 항목·받아쓰기·문제 풀이 시간). 그래서 요약에 적은 숫자와 화면의 숫자가 어긋나지 않습니다.
- `slug`는 `week-NN` 규칙을 지키세요. 예전 호(issue) 단위 기록을 주(week)로 이어받는 데 쓰입니다.
- 배열 순서는 상관없습니다 — `magazine.js`가 `week` 순으로 정렬해 씁니다. (파일에는 긴 발행분을 앞에 두었습니다.)
- `MAGAZINE_QUARTERS`가 플랜의 4분기 제목·설명을 담습니다.
- 기본으로 열리는 주는 `MAGAZINE_WEEKS`를 `week` 순으로 정렬한 **첫 주**(W01)이고,
  마지막으로 본 주는 브라우저에 기억됩니다.
- 섹션 필드는 모두 선택사항이고, 있는 필드만 그려집니다.

| 필드 | 형태 | 쓰임 |
| --- | --- | --- |
| `id` | `'quiz'` | 필수. 앵커·진행률 저장 키. 주 안에서 고유해야 합니다 |
| `kind` | `vocabulary` 등 | 분류. `mag.kind.<kind>` 문구와 짝을 이룹니다 |
| `level` | `A2`…`C1` | 난이도 표시 |
| `title` | `{ ko, en }` | 섹션 제목 |
| `intro` | `{ ko, en }` | 한 줄 도입 |
| `body` | `{ ko:[…], en:[…] }` | **영어가 본문**, 한국어는 번역 블록(가릴 수 있음) |
| `reading` | `[영어 문단, …]` | 독해 지문 |
| `items` | `[{ en, ko, note, meaning, example }]` | 어휘·표현. 저장 버튼과 발음 버튼이 자동으로 붙습니다 |
| `bullets` | `[{ ko, en }]` | 저장 버튼 없는 목록 |
| `table` | `{ caption, head:[{ko,en}], rows:[[…]] }` | 문법 표 |
| `dialogue` | `[{ who, en, ko }]` | 대화문. 줄마다 재생 버튼, 화자별로 다른 목소리 |
| `questions` | `[{ ko, en }]` | 토론 질문 |
| `dictation` | `[{ en, ko }]` | 듣고 받아쓰기 (오답 단어 표시) |
| `quiz` | `[{ q:{ko,en}, options:[…], answer:0, explain:{ko,en} }]` | 즉시 채점 + 점수 + 다시 풀기 |
| 〃 | 보기(`options`)는 영어 문자열이거나 `{ ko, en }` | 한국어 보기를 문자열로 넣으면 영어 모드에서 그대로 나옵니다 |
| `quote` | `{ ko, en }` | 섹션 중간의 강조 인용 |

- `kind` 값은 `mag.kind.<kind>` 문구와 짝을 이룹니다. 새 종류를 쓰려면 `script.js`의 `I18N`에
  `mag.kind.<이름>`을 ko/en 양쪽에 추가하세요.
- **집필 규칙**: 문자열은 작은따옴표로 감싸므로 값 안에 `'`를 쓰지 마세요(축약형은 풀어서).
  값 안에 `':` 조합도 피하세요 — 사전 검사 스크립트가 오인합니다.
  `**굵게**`만 지원합니다.
- **오디오**: `issues.js`의 영어 필드(`body.en`, `reading`, `items[].en`, `dialogue[].en`,
  `questions[].en`, `dictation[].en`)를 문장 단위로 잘라 `speechSynthesis`로 읽습니다.
  화자 이름(Front desk, You)은 **읽지 않고**, 목소리와 음높이로만 구분합니다.
  오디오 파일이 필요 없고 비용도 0입니다. "섹션 전체 듣기"에는 받아쓰기 문장도 포함됩니다.

## 다른 영어 학습 서비스에서 가져온 것

유료로 값을 하려면 "읽고 끝"이 아니라 **읽기 → 듣기 → 점검 → 복습**이 한 화면에서 닫혀야 합니다.
시중 서비스를 기능 단위로 비교해, 정적 사이트로 구현 가능한 것만 골라 담았습니다.

| 참고 서비스 | 그쪽의 핵심 | EngMon에 담은 방식 |
| --- | --- | --- |
| News in Levels | 같은 주제를 난이도별로 | 한 주 안에서 섹션마다 레벨 표시(A2~C1) |
| Engoo Daily News | 지문 + 어휘 + 토론 질문 + 표현 | 섹션 12~17개(어휘·구동사·연어·문법·발음·이디엄·회화·독해·쓰기·토론·퀴즈) |
| LingQ | 모르는 단어를 그 자리에서 저장 | 항목마다 "단어장에 저장" + 발음 듣기 |
| Anki / Quizlet | 간격 반복 복습 카드 | 단어장 기반 플래시카드(6단계 간격, `localStorage`) |
| Cake / 스픽 | 듣고 따라 말하기, 축약 발음 | 문장 단위 재생, 화자별 목소리, 축약 발음 섹션 |
| 받아쓰기 앱 | 들은 문장을 적고 채점 | 오답 단어를 짚어 주는 받아쓰기 섹션 |
| Duolingo | 즉시 채점 + 점수 | 확인 문제 즉시 채점, 점수 요약, 다시 풀기 |
| Blinkist류 구독 | 호 단위 목차·진행률 | 52주 로드맵, 읽기 진행률, 진행 바, 섹션 완료 표시 |
| 해커스/시원스쿨류 | 한/영 대조, 한국어 해설 | 영어 본문 + **번역 가리기** 토글, 한국어 해설 노트 |

## 로컬에서 보기

`index.html`을 브라우저로 열면 됩니다. 로컬 서버로 확인하려면:

```bash
python -m http.server 8000
# 또는
npx serve .
```

## 검증 (배포 전)

```bash
node smoke-test.js
```

브라우저 없이 페이지 스크립트를 **실제로 실행**해 보는 테스트입니다(의존성 없음, Node만 있으면 됩니다).
`index.html`의 인라인 스크립트 → `issues.js` → `script.js` → `magazine.js`를 최소 DOM 위에서 돌리고
언어 전환·테마·강조색·매거진 렌더링·주 전환·52주 플랜·번역 토글·받아쓰기 채점·퀴즈 점수·복습 카드·단어장 검색까지
클릭을 흉내 내 확인합니다. 현재 **63개 항목**을 검사합니다.

특히 다음을 지켜줍니다.

- **회귀 테스트**: 테마 초기화가 강제로 실패해도 언어 전환이 살아남는지 검사합니다.
- **홈 링크 회귀**: 상단 `홈`이 다른 사이트로 나가지 않고 이 페이지 안(`#top`)에 머무는지,
  그리고 스크롤 강조 대상에서 빠져 있는지 검사합니다.
  (늘 강조되면 다른 메뉴가 켜지지 않습니다.)
- **보기 언어**: 확인 문제 보기가 영어 문자열이거나 `{ ko, en }`인지 검사합니다.
  한국어 보기를 그냥 문자열로 넣으면 영어 모드에서 그대로 새어 나갑니다.
- **주 선택기**: 칩이 52주 모두 있고, 다른 주를 누르면 표지·목차·진행률이 그 주 기준으로 다시 그려지고 저장되는지 검사합니다.
- **52주 플랜**: 4분기 × 52주가 로드맵에 그려지고, 발행 예정 주가 48개인지 검사합니다.
- **발행 전인 주**: 본문 대신 "준비 중" 안내 1개만 나오고 목차·진행률·표지 날짜가 발행 예정으로 바뀌는지 검사합니다.
- **문의 유형**: 유형을 고르면 안내가 (필수)로 바뀌고 빈 내용은 막히는지,
  `type` 과 제목(`_subject`)이 함께 전송되는지, 관련 주 목록이 발행된 주 수만큼 채워지는지 검사합니다.
- **사전 검사**: `data-i18n` 키가 ko/en 양쪽에 모두 있는지, 한쪽에만 있는 키가 없는지 확인합니다.
- **데이터 검사**: 52주 플랜이 온전한지(중복 week 없음·분기 누락 없음), 모든 발행 주의 섹션 항목에 ko/en이 있는지,
  퀴즈 정답 범위·표 구조·받아쓰기 짝이 맞는지 확인합니다.
- **CSS 회귀**: 스티키 목차(`.issue-aside`)에 `max-height`와 `overflow-y:auto`가 남아 있는지,
  좁은 화면(≤760px)에서 섹션 머리(`.m-head`)가 줄바꿈되는지 검사합니다.
- **클래스 검사**: HTML과 렌더링 결과가 쓰는 클래스가 모두 `styles.css`에 있는지 검사합니다
  (새 UI를 만들고 스타일을 빠뜨리면 먼저 걸립니다).
- **문의 폼**: UTF-8 본문(`x-www-form-urlencoded`) 전송 값(`type`·`email`·`name`·`issue`·`message`·`_subject`·`source`),
  허니팟 칸이 비어 있는지, reCAPTCHA 키가 없을 때 **외부 요청이 0** 인지, 성공/실패/한도 초과(429) 문구,
  전송 후 입력값 초기화, 언어를 바꿨을 때 안내 문구가 함께 바뀌는지 확인합니다.
- **방문 분석**: 웹사이트 ID가 없을 때 **아무 요청도 보내지 않는지**, `file://`·추적 금지에서 멈추는지,
  켜졌을 때 Clarity 스크립트를 한 번만 부르고 주소에 프로젝트 ID가 들어가는지 확인합니다.

### 브라우저 검증

```bash
node browser-test.js          # 검증만
node browser-test.js --shots  # + .shots/ 에 스크린샷
```

헤드리스 Chrome을 띄워 **실제로 렌더링한 화면**을 잽니다(Chrome 경로는 `CHROME` 환경변수로 지정).

- 목차(사이드바)가 화면보다 길 때 잘리지 않고 **마우스 휠로 끝까지 내려가는지** (뷰포트 1080~600px)
- 모바일/태블릿 폭(820~320px)에서 가로 오버플로·잘림이 없는지, **폭 0으로 찌그러진 글상자가 없는지**
- 터치 스와이프 스크롤, 목차 항목 탭 이동
- 주 전환(52개 칩) · 52주 플랜 · 발행 전인 주 안내 · 오늘의 학습 · 단어장 · 복습 카드 · 인쇄 버튼 · 백업 파일 생성
- 인쇄 미디어에서 헤더·버튼이 숨고 한국어 해설은 남는지, 콘솔 에러가 없는지
- 방문 분석(Microsoft Clarity)이 ID 유무에 따라 **요청을 보내는지/안 보내는지**
  (모든 요청을 가로채 확인, Clarity로는 실제로 보내지 않음)

> `freePort()`로 빈 포트를 잡아 씁니다. 포트를 고정하면 **이전 실행의 Chrome이 남아 있을 때
> 그 인스턴스(옛 프로필)에 붙어** 검사 결과가 엉킵니다.

## 방문 분석 — Microsoft Clarity

누가 얼마나 들어오는지, 어디를 누르고 어디까지 읽는지 보려면 `analytics.js`가 있습니다.
[Microsoft Clarity](https://clarity.microsoft.com) 기준으로 맞춰 두었습니다.

**현재 연결 상태:** engmon.monster → 프로젝트 ID `ymt22k85ah` (**켜짐**).
ID를 `''`로 비우면 다시 꺼지고, 그 때는 어떤 요청도 나가지 않습니다.

**무료이고 사이트·트래픽 제한이 없습니다.** 무엇보다 **한 프로젝트(하나의 ID)로 여러 도메인을 함께** 볼 수 있어,
`engmon.monster` · `monsterlab.monster` · `toeic.monster`를 한 대시보드에서 봅니다.
(Umami Cloud 무료 Hobby는 웹사이트 1개까지만 되어 도메인이 여러 개면 부족합니다.)

### 켜는 법

1. [clarity.microsoft.com](https://clarity.microsoft.com)에서 계정을 만들고 **Add new project** 로 프로젝트를 만듭니다
   (사이트 주소는 `engmon.monster`).
2. **Settings → Overview → Project ID** 를 복사합니다(예: `kwq1z2abcd`).
3. `index.html`의 한 줄에 붙여 넣고 배포합니다. 그게 전부입니다.

```html
<script>window.ENGMON_CLARITY_ID = '여기에-프로젝트-id';</script>
```

> ID를 정하는 곳은 이 한 곳뿐입니다. `analytics.js`가 공식 스니펫을 대신 넣습니다
> (직접 `<script>`를 넣지 마세요 — 두 번 부르게 됩니다). 데이터가 보이기까지 몇 분 걸립니다.

### 도메인이 여러 개면 같은 ID 하나로

Clarity 프로젝트는 **여러 사이트를 묶습니다.** 각 사이트(`monsterlab.monster` · `toeic.monster`)의
`index.html`에도 **같은 Project ID** 를 넣으면 한 대시보드에서 함께 봅니다.
도메인별로 따로 보고 싶으면 프로젝트를 각각 만들어 다른 ID를 넣으면 됩니다.

### 켤 때 대시보드에서 함께 설정할 것 (권장)

| 설정 | 위치 | 왜 |
| --- | --- | --- |
| **Masking → Strict** | Settings → Masking | 화면의 모든 글자를 가립니다. 단, **입력창·드롭다운 내용은 어느 모드에서도 기록되지 않습니다** |
| **Cookies 끄기** | Settings → Setup → Advanced | 쿠키(`_clck`·`_clsk`)를 안 쓰면 동의 배너 없이도 됩니다 |
| **동의 신호 연결** | consent API | 유럽(EEA·영국·스위스) 방문자가 있으면 필요합니다(2025-10-31부터 강제). 없으면 세션 리플레이·퍼널이 제한됩니다 |

### 안 켜지는 경우 (의도된 동작)

| 상황 | 왜 |
| --- | --- |
| ID가 비어 있거나 자리표시자(`xxxxxxxxxx`)·형식 오류 | 실수로 남의 프로젝트에 보내는 일을 막습니다 |
| `file://`로 열었을 때 | 로컬 확인이 통계에 섞이지 않게 합니다 |
| 브라우저의 추적 금지(Do Not Track)가 켜짐 | **Clarity 자체는 DNT를 따르지 않습니다.** 그래서 우리가 아예 불러오지 않습니다 |

이 경우 **스크립트를 내려받지도, 요청을 보내지도 않습니다.** (브라우저 검증으로 확인합니다)

### 무료 분석 도구 비교 (2026-09 확인)

| 도구 | 무료 범위 | 여러 사이트 | 쿠키·동의 배너 | 성격 |
| --- | --- | --- | --- | --- |
| **Cloudflare Web Analytics** | 사이트·트래픽 제한 사실상 없음 | **미프록시 10개까지**(Cloudflare 프록시를 쓰면 무제한) | 없음(쿠키·localStorage 안 씀) | 페이지뷰·방문·유입·국가·Core Web Vitals. 대시보드 6개월 보관 |
| **Umami 자체 호스팅** | 완전 무료(서버 비용만) | 무제한 | 없음 | Umami Cloud와 같은 화면. 사이트별 `data-website-id`만 다르게 |
| **Matomo (On-Premise)** | 완전 무료(오픈소스) | 무제한 | 없음(설정으로 선택) | 기능이 가장 많음. PHP+MySQL 서버 필요 |
| **GoatCounter** | 개인·중소 사이트는 무료 호스팅 | 계정 하나에 여러 사이트 | 없음(쿠키 안 씀) | 가볍고 단순. 3.5KB 스크립트 |
| **Microsoft Clarity** ← 지금 연결됨 | 무료, 사이트·트래픽 제한 없음 | 무제한(**ID 하나로 여러 도메인 묶음**) | 쿠키 사용(설정에서 끌 수 있음) → EEA·영국은 동의 필요 | 히트맵·세션 리플레이. **입력창 내용은 기록 안 함**. 보관: 재생 30일 · 히트맵/클릭 9개월 |
| **PostHog** | 월 100만 이벤트 + 세션 리플레이 5천 | 프로젝트 여러 개 | 기본은 쿠키·localStorage 사용 | 제품 분석·세션 리플레이. 정적 매거진에는 과함 |
| Plausible · Fathom · Simple Analytics | **무료 없음**(유료 월 $9~, 30일 체험) | 플랜에 따라 | 없음 | 가장 깔끔하지만 유료 |

**고를 때 기준**

- **지금(Clarity)** — 무료이고 도메인이 여러 개이며, 어디를 누르는지·어디까지 읽는지까지 보고 싶을 때.
  대신 쿠키와 30일 보관, 세션 리플레이를 함께 받아들입니다.
- **쿠키를 절대 쓰기 싫다면** — Cloudflare Web Analytics(미프록시 10개까지, 쿠키 없음)
  또는 Umami 자체 호스팅(무제한, 서버 필요).
- **지표가 단순해도 된다면** — GoatCounter.

### 무엇을 보내나

페이지 방문 · 클릭 · 스크롤 · 마우스 움직임 · 히트맵 · 세션 리플레이.

- **입력창·드롭다운에 적은 값은 어느 마스킹 모드에서도 기록되지 않습니다.** 받아쓰기 답·이메일·이름은 가지 않습니다.
- 보관 기간: **세션 재생 30일**, **히트맵·클릭 데이터 9개월**(★표시한 세션은 9개월).
  만 18세 미만을 대상으로 하는 사이트에는 쓰지 마세요.
- 우리 코드가 **학습 데이터(섹션 완료·단어 저장·정답률)를 보내는 호출은 하지 않습니다.**
  다만 세션 리플레이 특성상 클릭·스크롤은 화면 기록으로 남습니다 — 그래서 **Masking 은 Strict 를 권합니다.**
- 학습 기록(단어장·진행률)은 여전히 **이 브라우저 안에만** 남습니다 (`localStorage`).

### 다른 도구로 바꾸려면

`analytics.js`가 하는 일은 스크립트 태그를 하나 붙이는 것뿐입니다.
Cloudflare Web Analytics · Umami 자체 호스팅 · GoatCounter 같은 도구도
`analytics.js`의 주소 한 줄과 넘기는 값만 바꾸면 됩니다.
끄고 싶으면 ID를 `''`로 비우세요 (파일은 그대로 둡니다).

### 확인하는 법

```bash
node smoke-test.js     # 무동작 7가지 + file://·추적금지 차단 + 스크립트 1회·ID 전달 검사
node browser-test.js   # 실제 네트워크 요청 검사 (7번 항목)
```

## 문의 폼 (Formspree)

「새 주 알림」 섹션(`#next`)의 신청 폼은 별도 서버 없이 [Formspree](https://formspree.io)로 전송됩니다.

**현재 연결 상태:** engmon.monster → `https://formspree.io/f/mnpnvqaq`

- 수신처를 바꾸려면 **`index.html` 의 `<form ... action>` 한 줄**의 폼 ID만 바꾸면 됩니다.
- **문의 유형** (`type`): `subscribe` 새 주 알림 · `content` 오류·오타 제보 · `topic` 주제·주차 제안 ·
  `study` 학습 방법 질문 · `partner` 제휴·광고 · `etc` 그 외. 유형을 고르면 메일 제목이
  `EngMon — <유형> · <관련 주>` 로 바뀝니다(제목은 늘 `EngMon` 으로 시작해야 수신함에서 바로 보입니다).
- **내용이 필요한 유형**: 새 주 알림 신청(`subscribe`)만 내용을 비워 둘 수 있습니다.
  나머지 유형은 내용이 비어 있으면 전송하지 않고 안내 문구를 띄웁니다(빈 메일은 답장할 수 없습니다).
- **관련 주** (`issue`): 목록을 `issues.js`를 보고 `magazine.js`가 채웁니다. **발행된 주만** 넣습니다
  (오류·오타 제보는 공개된 본문에만 할 수 있습니다). 주를 발행하면 자동으로 늘어납니다.
- 보내는 값: `type` · `email`(답장 주소 — Formspree가 회신 주소로 씁니다) · `name`(선택) ·
  `issue`(관련 주 slug, 선택) · `message`(선택) ·
  `_subject`(숨은 필드, 스크립트가 없으면 이 값이 그대로) · `source`(접속 도메인).
  reCAPTCHA를 켜면 `g-recaptcha-response`가 더해집니다.
- **한글이 깨져 도착할 때**: 이 폼은 항상 UTF-8로 보냅니다. 그런데 PowerShell·cmd 터미널에서
  `curl`·스크립트로 시험 전송하면 Windows 콘솔 문자셋(CP949)으로 인코딩되어 메일이 깨져
  도착합니다(이모지는 `?` 로 바뀝니다). 확인은 브라우저에서 폼에 직접 입력해 보내세요.
- 전송 형식은 **`x-www-form-urlencoded` + `charset=UTF-8`** 입니다. 폼 값을 `URLSearchParams` 로
  UTF-8 퍼센트 인코딩해 보내고 `Content-Type` 에 문자셋을 적습니다.
  (예전에는 `FormData`(multipart)를 썼는데, multipart 본문에는 "이 본문은 UTF-8" 이라는 표시가
  없어 수신 쪽이 EUC-KR/CP949 로 해석하면 한글 문의가 깨져 도착했습니다.
  `x-www-form-urlencoded` 는 CORS 안전 헤더라 사전 요청(preflight)도 생기지 않습니다.
  스크립트 없이 전송되는 경우를 위해 `<form>` 에 `accept-charset="UTF-8"` 도 함께 적어 두었습니다.)
- **스팸 방지 기본값**: 숨은 함정 칸(`name="_gotcha"`)을 화면 밖으로 밀어 두었습니다.
  봇이 이 칸을 채우면 Formspree가 제출을 조용히 버립니다(사람에게는 보이지 않고 탭 순서에서도 빠집니다).
- **reCAPTCHA v3 (선택)**: `<form data-recaptcha-key="">` 에 사이트 키를 넣으면 켜집니다
  (같은 키의 비밀 키를 Formspree 폼 설정에 넣어야 합니다). 켠 경우에만 첫 전송 때 Google 스크립트를
  한 번 불러오고 토큰을 붙입니다 — **비워 두면 외부 요청이 0** 입니다(방문 분석과 같은 방식).
- `script.js` 의 `guard('문의 폼')` 블록이 `fetch` 로 전송(`Accept: application/json`)을 맡습니다.
  **스크립트가 없어도 동작합니다** — 그때는 브라우저가 `action` 으로 그대로 POST 하고
  Formspree 안내 페이지가 뜹니다. 전송 중에는 버튼을 잠그고(Formspree 는 분당 20건 제한),
  결과를 `#formStatus` 에 표시합니다. `429` 는 "잠시 후 다시 시도" 문구로 구분합니다.
- 전송이 안 되면 옆의 **「메일 보내기」(mailto)** 버튼으로 같은 내용을 보낼 수 있습니다.
- 대시보드에서 **Restrict to domain → `engmon.monster`** 를 넣어 두면 다른 도메인에서 오는 제출이 스팸함으로 갑니다.
  (이 설정은 프로젝트당 도메인 한 개입니다. 무료 플랜도 쓸 수 있습니다 — 폼·프로젝트 무제한, 월 50건 합산.)

## 저장 키 (모두 이 브라우저에만)

`localStorage`에만 남고 서버로 보내지 않습니다. (기존 MonsterLab 키 이름을 그대로 씁니다.)

`monsterlab.wordbook`(저장한 단어) · `monsterlab.progress`(섹션 완료, `week-01` 기준) ·
`monsterlab.srs`(복습 단계) · `monsterlab.daily`(오늘 활동 수) · `monsterlab.days`(학습한 날짜) ·
`monsterlab.issue`(마지막으로 본 주) · `monsterlab.translation`(번역 표시) · `monsterlab.rate`(듣기 속도) ·
`monsterlab.lang`(화면 언어) · `monsterlab.theme`(테마) · `monsterlab.accent`(강조색)

단어장 화면의 **전체 백업(JSON)** / **백업 가져오기**로 이 데이터를 파일 하나에 담아 옮길 수 있습니다.
예전 `issue-01` 형식으로 저장된 기록은 처음 열 때 `week-01`로 자동으로 옮겨집니다.

## 학습 도구

- **읽기**: 주별 읽기 진행률·진행 바, 섹션별 예상 학습 시간(읽기 + 문제 풀이), 섹션 완료 표시, 이어서 읽기
- **듣기**: 섹션 전체 듣기(받아쓰기 문장 포함), 대화 전체 듣기, 줄 단위 듣기, 항목별 발음 듣기, 3단계 속도(0.75× / 1× / 1.25×)
- **번역 가리기**: 한국어 도움말(`.trl`)을 한 번에 감춥니다. 영어만 보고 뜻을 떠올리는 연습용
- **받아쓰기**: 문장을 입력해 채점하고, 놓친 단어를 빨갛게 표시하며 정답을 보여 줍니다
- **확인 문제**: 즉시 채점, 정답률 요약, 다시 풀기
- **답 유지**: 언어(한/영)를 바꿔 본문을 다시 그려도 고른 답·점수·받아쓰기 입력이 그대로 남습니다
- **복습 카드**: 단어장에 담은 표현을 6단계 간격(0·1·3·7·16·35일)으로 다시 봅니다
- **단어장**: 저장·검색·복사·CSV 내보내기·전체 백업/복원(JSON)·비우기
- **주 선택기**: 52주 칩으로 주를 오가며 보고, 마지막으로 본 주를 기억합니다 (주마다 진행률·완료 표시가 따로 쌓입니다)
- **52주 플랜**: 4분기 로드맵에서 앞으로의 주제를 미리 보고 바로 그 주로 이동합니다
- **오늘의 학습**: 오늘 몇 개 활동을 했는지(단어 저장·섹션 완료·정답) 목표 3개 기준으로 보여 주고,
  최근 7일 점과 **연속 학습일·최고 기록**을 함께 표시합니다
- **인쇄 · PDF 저장**: 표지의 버튼으로 그 주 전체를 종이 한 부로 만듭니다 (학습 도구는 숨기고 한국어 해설은 되살림)

## 접근성·사용성

- 한/영 전환 토글 — 동적 콘텐츠도 `langchange` 이벤트로 함께 다시 그려집니다 (**Alt + L**)
- 다크/라이트/시스템 3단계 테마 **선택** 버튼, 강조색 4종 프리셋 (모두 `localStorage` 저장)
- 영어 지문에 `lang="en"`, 한국어 해설에 `lang="ko"` — 화면 낭독기와 브라우저 음성이 올바른 언어로 읽습니다
- 모바일 햄버거 메뉴, 스크롤 시 헤더 경계선, 맨 위로 가기, 키보드 포커스 링
- `prefers-reduced-motion` 존중
- 사이드바(목차)가 화면보다 길면 **그 안에서 스크롤**할 수 있습니다 (아래 항목이 잘리지 않도록)
- 좁은 화면에서는 목차가 본문 위로 내려가고, 섹션 제목·학습 도구가 두 줄로 나뉘어 글자가 찌그러지지 않습니다

## SEO·공유

- `og:title`·`og:description`·`og:image`(`og.png`, 1200×630)·`twitter:card` 메타가 있습니다.
- `robots.txt`가 크롤링을 허용하고 `sitemap.xml` 위치를 알려 줍니다. `sitemap.xml`은 한 페이지(`/`)만 담습니다
  (주는 한 문서 안에서 전환하므로 별도 URL이 아닙니다) — `changefreq`는 `weekly`입니다.
- 주 단위로 내용이 바뀌므로, 검색엔진에는 주기적 색인이 자연스럽습니다.

## 캐시 무효화 (배포 후 "안 바뀐 것처럼 보이는" 문제)

CSS·JS를 참조할 때 `?v=숫자` 같은 버전을 붙여 둡니다(파일마다 따로 올립니다).

```html
<link rel="stylesheet" href="styles.css?v=10" />
<script src="script.js?v=11"></script>
```

**CSS나 JS를 고쳐서 배포할 때는 `index.html`의 `?v=` 숫자를 올리세요.**
그러지 않으면 브라우저나 CDN이 예전 파일을 계속 쓰면서 수정이 반영되지 않은 것처럼 보입니다.

## 배포

정적 호스팅 아무 곳에나 파일을 올리면 됩니다
(`index.html`, `issues.js`, `magazine.js`, `script.js`, `analytics.js`, `styles.css`,
`og.png`, `robots.txt`, `sitemap.xml`, `CNAME`).

- **GitHub Pages**: 이 저장소를 푸시하고 Pages를 활성화 (커스텀 도메인은 `engmon.monster`)
- **Vercel / Netlify / Cloudflare Pages**: 빌드 명령 없이 루트 디렉터리 지정

> GitHub Pages는 저장소당 커스텀 도메인 하나만 지원합니다. 그래서 MonsterLab(`monsterlab.monster`)과
> EngMon(`engmon.monster`)은 **저장소를 분리**해 운영합니다.

## 브랜드

- 이름: **EngMon** (한글 **엥몬**), 도메인 `engmon.monster` — `eng`(English) + `mon`(Monster) + `.monster`
- MonsterLab(`monsterlab.monster`)의 형제 브랜드. 같은 `.monster` 패밀리입니다.
- 컨셉: **영어를 먹고 자라는 괴물**. 매 주마다 진화하며, 읽기 진행률이 곧 성장입니다.
- 색·테마는 `styles.css`의 기존 변수를 그대로 씁니다 (다크 `#0a0e13` + 강조색 4종).

### 이메일 주소

수신 주소는 **`kaist2718@gmail.com`** 입니다. `script.js`의 `EMAIL` 상수와 `index.html`의 `mailto:` 링크에 나옵니다.

### 색상 / 테마 / 강조색

`styles.css` 상단의 변수 블록(`:root`, `:root[data-theme="light"]`, `:root[data-accent="..."]`)을 따릅니다.
라이트 테마의 `--accent`는 본문 텍스트로도 쓰이므로 **배경 대비 4.5:1**을 지켜야 합니다.
